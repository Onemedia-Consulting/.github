name: Task
description: Track a general task, chore, or improvement
labels: ["task"]
body:
  - type: markdown
    attributes:
      value: |
        Use this template for general work that is neither a bug nor a feature request.

        **No task is "ready" without acceptance criteria** — even chores get checkable done-conditions.

  - type: textarea
    id: description
    attributes:
      label: Description
      description: What needs to be done?
    validations:
      required: true

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: Acceptance Criteria
      description: Checkable, observable done-conditions. How do we know this task is complete?
      value: |
        - [ ] 
        - [ ] 
    validations:
      required: true

  - type: textarea
    id: out_of_scope
    attributes:
      label: Out of scope
      description: What this explicitly does NOT cover.
    validations:
      required: false

  - type: dropdown
    id: category
    attributes:
      label: Category
      options:
        - Infrastructure
        - Documentation
        - Refactoring
        - Dependency update
        - Security hardening
        - Performance
        - Tooling / CI
        - Other
    validations:
      required: true

  - type: checkboxes
    id: security_considerations
    attributes:
      label: Security considerations
      options:
        - label: Modifies security controls or policies
        - label: Affects access management or secrets
        - label: Modifies authentication or authorization
        - label: No security implications
